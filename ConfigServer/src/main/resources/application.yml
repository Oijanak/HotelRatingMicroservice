server:
  port: 8087
  
spring:
  application:
    name: CONFIG-SERVER
   
  cloud:
    config:
      server:
        git:
          uri: https://github.com/Oijanak/Config-MicroServices
          clone-on-start: true
eureka:
  client:
    service-url:
      defaultZone: http://localhost:8088/eureka